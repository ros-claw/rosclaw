schema_version: rosclaw.simforge.scenario_distribution.v1
variables:
  fault:
    distribution: choice
    values: [bridge_disconnect, command_drop, ack_drop, observation_drop, observation_delay, stale_observation, malformed_observation, duplicate_command, daemon_restart]
  delay_ms: {distribution: choice, values: [0, 10, 100, 1000]}
constraints: []
