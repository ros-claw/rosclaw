core:
  pip:
    - "lerobot"
  checks:
    - "lerobot-info"
  enabled_capabilities:
    - "provider_type_lerobot_policy"
    - "dataset_export_lerobot"

dataset:
  pip:
    - "lerobot"
  checks:
    - "lerobot-info"
  enabled_capabilities:
    - "dataset_export_lerobot"

train:
  pip:
    - "lerobot"
  checks:
    - "lerobot-info"
  enabled_capabilities:
    - "provider_type_lerobot_policy"
    - "dataset_export_lerobot"
    - "train"

eval:
  pip:
    - "lerobot"
  checks:
    - "lerobot-info"
  enabled_capabilities:
    - "eval"

rollout:
  pip:
    - "lerobot"
  checks:
    - "lerobot-info"
  enabled_capabilities:
    - "rollout"

reward:
  pip:
    - "lerobot"
  checks:
    - "lerobot-info"
  enabled_capabilities:
    - "reward"

all:
  pip:
    - "lerobot"
  checks:
    - "lerobot-info"
